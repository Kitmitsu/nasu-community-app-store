version: '3.7'
services:
  app_proxy:
    environment:
      APP_HOST: nasu-suwayomi_server_1
      APP_PORT: 3000
    container_name: nasu-suwayomi_app_proxy_1
  server:
    image: >-
      ghcr.io/suwayomi/suwayomi-server:preview
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/home/suwayomi/.local/share/Tachidesk:rw
    environment:
      TZ: Europe/London
      PUID: 1000
      PGID: 1000
    container_name: nasu-suwayomi_server_1
